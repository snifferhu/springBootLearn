spring:
  application:
    name: configSpace
  cloud:
    config:
      uri: http://localhost:7001
      discovery:
        enabled: true

eureka:
  instance:
    configServer:
      metadataMap:hju
        configPath: /configSpace

encrypt:
  failOnError: false